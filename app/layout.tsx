import type { Metadata } from "next";
import { Space_Grotesk, Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const spaceGrotesk = Space_Grotesk({
  variable: "--font-display",
  subsets: ["latin"],
  display: "swap",
});

const inter = Inter({
  variable: "--font-body",
  subsets: ["latin"],
  display: "swap",
});

const jetbrainsMono = JetBrains_Mono({
  variable: "--font-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Losing Weight is Math | Track Calories, Lose Weight",
  description:
    "Weight loss isn't magic. It's math. Track calories in vs calories out with AI-powered food logging and watch your real weight trend down.",
  keywords: [
    "weight loss",
    "calorie tracking",
    "fitness app",
    "diet tracker",
    "AI food logging",
  ],
  openGraph: {
    title: "Losing Weight is Math",
    description: "Weight loss isn't magic. It's math.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${spaceGrotesk.variable} ${inter.variable} ${jetbrainsMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
